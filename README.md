# Silence End

When rendering external instrument to samples, the latency is not always well
managed. This tool allows moving the silence at the beginning to the end

## Features

We add two right-click menu entries:

- in `sample editor > process`: processes current sample
- in `instrument list`: processes all samples in the current instrument

## Credits

This code is adapted from `hack.dpp.hwsampler`.